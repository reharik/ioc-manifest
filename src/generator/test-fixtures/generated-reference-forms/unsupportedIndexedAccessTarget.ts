// REJECTED: only `IocGeneratedCradle` carries keys generation can resolve syntactically.
import type { IocExternals } from "./generated/ioc-registry.types.js";

export type Config = IocExternals["config"];
