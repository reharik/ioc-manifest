import type { EmailChannel, SmsChannel } from "./channel-contracts.js";

/** Stale generated cradle: `channels` is unknown until regen fixes it. */
export interface IocGeneratedCradle {
  albumRepository: unknown;
  channels: unknown;
  emailChannel: EmailChannel;
  smsChannel: SmsChannel;
}
