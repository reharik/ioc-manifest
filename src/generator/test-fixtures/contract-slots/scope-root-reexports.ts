/** Re-exports so the opener-key offender can name both contracts without a second import path. */
export type { RequestPipeline } from "./contracts.js";
export type { SlotRouter } from "./scope-root.js";
