import type { MarkerBase, Mid } from "./base-types.js";

/** Barrel alias for marker config (re-exported marker base). */
export type ScopedMarker = MarkerBase;

/** Barrel alias for group baseType config. */
export type MidRef = Mid;
